---
标题：“基础知识”
描述：“”
图标：“播放”
模式：“广角”
---


```python
from browser_use import Agent, ChatBrowserUse

agent = Agent(
    task="Search for latest news about AI",
    llm=ChatBrowserUse(),
)

async def main():
    history = await agent.run(max_steps=100)
```

- “任务”：你想自动化的任务。
- 'LLM'：你最喜欢的LLM。参见 <a href=“/customize/supported-models”>Supported Models</a>。

该代理通过异步 'run（）' 方法执行：

- “max_steps”（默认：“100”）：代理可采取的最大步数。

请查看所有可自定义参数<a href = “/customize/agent/all-parameters”></a>。